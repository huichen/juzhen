CS c1[]= { {1., 2.}, {-3., -1.}, {2.0, 0.1}, {-2., 0.1}, {6., 7.}, {3.0, 0.1}};
cvector v1(c1, 6);

V(v1, "{(1, 2), (-3, -1), (2, 0.1), (-2, 0.1), (6, 7), (3, 0.1)}")

V(Real(v1), "{1, -3, 2, -2, 6, 3}")

V(Imag(v1), "{2, -1, 0.1, 0.1, 7, 0.1}")

V(Transpose(v1), "{(1, 2), (-3, -1), (2, 0.1), (-2, 0.1), (6, 7), (3, 0.1)}")

V(Conjugate(v1), "{(1, -2), (-3, 1), (2, -0.1), (-2, -0.1), (6, -7), (3, -0.1)}")

V(Adjoint(v1), "{(1, -2), (-3, 1), (2, -0.1), (-2, -0.1), (6, -7), (3, -0.1)}")

VME(v1, Conjugate(Adjoint(v1)))

VME(Adjoint(v1), Conjugate(Transpose(v1)))

double a[] = {1., 2., 3., 4.};
svector v2(a, 4);

V(v2, "{1, 2, 3, 4}")

V(Conjugate(v2), "{1, 2, 3, 4}")

V(Transpose(v2), "{1, 2, 3, 4}")

V(Adjoint(v2), "{1, 2, 3, 4}")

V(v1.Block(1, 4), "{(-3, -1), (2, 0.1), (-2, 0.1)}")

cvector v3;
v3 = v1.Transpose();
V(Transpose(v3), "{(1, 2), (-3, -1), (2, 0.1), (-2, 0.1), (6, 7), (3, 0.1)}")

v3 = v1.Conjugate();
V(v3, "{(1, -2), (-3, 1), (2, -0.1), (-2, -0.1), (6, -7), (3, -0.1)}")

v3 = v1.Adjoint();
V(v3, "{(1, -2), (-3, 1), (2, -0.1), (-2, -0.1), (6, -7), (3, -0.1)}")

svector v4 =  v2;
V(v4.Swap(1, 2), "{1, 3, 2, 4}")

v4 = v2;
v4.Swap(1, 2);
V(v4, "{1, 3, 2, 4}")

V(Join(v2, v4), "{1, 2, 3, 4, 1, 3, 2, 4}")

V(svector(STLVector(v2)), "{1, 2, 3, 4}")

const double a1[] = {1, -3, -2, 4};

dvector v5(a1, 4);
zvector v6(a1, 4);

V(Abs(v5), "{1, 3, 2, 4}")
V(Abs2(v5), "{1, 9, 4, 16}")

V(Abs(v6), "{1, 3, 2, 4}")
V(Abs2(v6), "{1, 9, 4, 16}")
